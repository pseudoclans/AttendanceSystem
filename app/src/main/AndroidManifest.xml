<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:networkSecurityConfig="@xml/network_security_config"

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/qcu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/qcu"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity android:name=".studentView"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".MainActivity2"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".adminView"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".profView"/>
        <activity android:name=".AdminViewStudentOne"
            android:windowSoftInputMode="adjustPan"/>


        <activity


        android:name=".adminViewProfessorOne"
            android:exported="true"

            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>